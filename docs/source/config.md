# Configuration

```{eval-rst}
.. currentmodule:: arize.config
.. autoclass:: SDKConfiguration
   :members:
   :member-order: groupwise
   :exclude-members: api_key, api_host, api_scheme, otlp_host, otlp_scheme, flight_host, flight_port, flight_scheme, pyarrow_max_chunksize, request_verify, stream_max_workers, stream_max_queue_bound, max_http_payload_size_mb, arize_directory, enable_caching, region, single_host, single_port

.. currentmodule:: arize.regions
.. autoclass:: Region
   :members:
   :member-order: groupwise
```
